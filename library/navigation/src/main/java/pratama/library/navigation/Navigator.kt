package pratama.library.navigation

import androidx.navigation.NavController

class Navigator {
    lateinit var navController: NavController

//    fun navigatoToFlow(navigationFlow: NavigationFlow) = when (navigationFlow) {
////        NavigationFlow.HomeFlow ->navController.navigate()
//
//    }
}